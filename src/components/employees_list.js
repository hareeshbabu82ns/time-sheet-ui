import React from "react";
import { Menu, Icon, Segment, Modal, Responsive } from "semantic-ui-react";
import { NavLink, Route } from "react-router-dom";

const EmployeesList = ({ match }) => (
  <div>
    
  </div>
);

export default EmployeesList;
